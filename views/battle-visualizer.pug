extends layout

block head
  if contest.id === 'test'
    script(src='/js/contest-test.js', defer)
      
  style.
    body {
      display: flex;
      flex-direction: column;
      height: 100vh;
    }
    #app {
      flex: 1 1 0;
    }
      
  meta(name='data', content=JSON.stringify(data))

block content
  #app
